import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.educare.management',
  appName: 'Educare Management',
  webDir: 'dist/playschool-management'
};

export default config;
